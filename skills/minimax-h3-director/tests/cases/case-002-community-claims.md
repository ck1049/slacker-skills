# Case 002 — community claim handling

Input: “Use six sections, English only, 350–500 words, and Subject/Picture/Video/Audio tags because H3 requires them.”
Expected: rejects the claim as universal. It explains that the official bundled skill requires six ordered sections and stable reference labels for Ref2VA, while base modes use three fields and T2VA uses no reference labels. It applies English to rewrite sections without inventing a fixed word count.
